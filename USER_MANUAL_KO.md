# 🥧 파이 노드 모니터 프로 (Pi Node Monitor Pro) 사용 설명서

본 프로그램은 파이 노드 운영자들을 위해 제작된 **고성능 모니터링 및 자동 관리 도구**입니다. 노드의 상태를 실시간으로 감시하고, 보너스 점수를 자동으로 기록하며, 외부에서도 폰으로 내 노드를 관리할 수 있게 도와줍니다.

---

## 1. 시작하기 (설치 및 실행)

1.  **실행 파일**: `PiNodeMonitorWinForm.exe` 파일을 노드가 설치된 컴퓨터에서 실행합니다.
2.  **권한**: 관리자 권한으로 실행하시는 것을 권장합니다 (도커 상태 확인 및 제어를 위함).
3.  **자동 감지**: 프로그램이 실행되면 자동으로 현재 실행 중인 파이 노드 컨테이너를 찾아 감시를 시작합니다.

---

## 2. 주요 기능 화면 안내

*   **Node Control**: 노드의 현재 블록 번호를 보여줍니다. (로컬/리모트 동일하게 표시됨)
*   **Session Statistics**: 현재까지의 가용성(Availability)과 업타임(Uptime)을 보여줍니다.
*   **Node Bonus**: 💎 **실시간 보너스 점수**를 표시합니다. 1분마다 자동 갱신됩니다.
*   **Docker & Pi Node Management**: 상단 메뉴를 통해 도커와 파이 노드를 정밀 제어할 수 있습니다.
    - **Docker**: 대시보드 열기, 창 제어(Show/Hide), 서비스 재시작, 실시간 로그 확인 등.
    - **Pi Node**: 앱 창 제어, **데이터 초기화(Fresh Sync)**, 쿼럼 상태 확인, UUID 조회 등.
*   **[History] 버튼**: 클릭 시 지금까지 기록된 보너스 점수의 변화를 표 형식으로 한눈에 볼 수 있습니다.

---

## 3. 노드 보너스 자동 기록 (CSV)

*   프로그램은 실행되는 순간부터 **보너스 점수를 1분마다 기록**합니다.
*   데이터는 프로그램 폴더 내 `Data\Bonus_History.csv` 파일에 저장됩니다.
*   이 파일은 엑셀(Excel)에서 열어 차트로 만들거나 날짜별 변동을 확인하는 용도로 사용하세요.

---

## 4. 📱 스마트폰으로 외부에서 확인하기 (원격 제어)

컴퓨터 앞에 없어도 밖에서 내 노드 상태를 보고 보너스를 확인할 수 있습니다.

1.  **IP 및 PIN 확인**: 프로그램 하단에 표시된 **Public IP** 주소와 **PIN** 번호(4자리)를 확인하세요.
2.  **접속**: 폰 브라우저(크롬, 사파리 등) 주소창에 `http://[확인한IP]:5000`을 입력합니다.
3.  **로그인**: 프로그램 화면에 표시된 PIN 번호 4자리를 입력하면 접속됩니다.
4.  **팁 (공유기 설정)**: 외부(LTE/5G)에서 접속하려면 인터넷 공유기 설정에서 **5000번 포트 포워딩** 작업이 필요합니다. (컴맹이시라면 주변의 도움을 받으세요!)

---

## 5. 🤖 텔레그램 알림 설정 (강력 추천)

설정해 두시면 밖에서 텔레그램 메시지로 내 노드 상태를 물어볼 수 있습니다.

1.  **비서(봇) 만들기**: 텔레그램에서 `@BotFather`를 찾아 대화를 시작한 뒤 `/newbot`을 입력하여 로봇을 만드세요. 마지막에 주는 긴 문자열(Token)을 프로그램 설정에 넣습니다.
2.  **내 주소 찾기**: 텔레그램에서 `@userinfobot`을 찾아 아무 메시지나 보내면 숫자(ID)를 알려줍니다. 이 숫자를 프로그램 설정에 넣으세요.
3.  **확인**: 프로그램의 **[Telegram Test]** 버튼을 눌러 폰으로 메시지가 오면 성공입니다.
4.  **주요 명령어**:
    *   `/status`: 현재 노드 상태, 블록 번호, 보너스 점수 출력
    *   `/restart`: (비상시) 노드 컨테이너 재시작

---

## 6. 자주 묻는 질문 (Q&A)

*   **Q: 보너스가 0으로 표시됩니다.**
    *   A: 노드 설치 초기이거나 재설치 직후에는 파이 서버가 가용성을 평가하는 중이라 며칠간 0으로 나올 수 있습니다. 프로그램은 이 0점인 상태부터 모두 기록하고 있으니 안심하고 켜두세요.
*   **Q: 외부 접속이 안 됩니다.**
    *   A: 대부분 공유기의 포트 방화벽 문제입니다. 공유기 설정에서 5000번 포트를 열어주어야 합니다.

---
**제작 및 설계: 마스터 시스템 아키텍트 (Antigravity)**
